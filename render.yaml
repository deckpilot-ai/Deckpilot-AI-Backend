# ⚠️  RENDER DEPLOYMENT IS DISABLED
#
# DeckPilotAI Backend is deployed EXCLUSIVELY to AWS Lightsail via GitHub Actions.
# Deployment workflow: .github/workflows/deploy-lightsail.yml
#
# ACTION REQUIRED (one-time, in Render dashboard):
#   1. Go to https://dashboard.render.com
#   2. Find the "deckpilotai-backend" service
#   3. Click Settings → "Suspend Service" (or Delete if no longer needed)
#
# DO NOT re-enable autoDeploy or reconnect this repository to Render.
# To deploy: push to the `deploy-dev` branch — GitHub Actions handles the rest.

# Intentionally empty — no Render services defined.
services: []
